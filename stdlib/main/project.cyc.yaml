source: "cyclic/"
output: "out/classes/"

jdk: 17
# We *are* the cyclic lib, don't try to load another one
cyclicLib: -1

includeDebug: true

packages:
- name: "cyclic_lib"
  version: "0.0.1"
  type: "jar"
  location: "out/libs/"