source: "cyclic/"
output: "../../build/classes/cyclic/"

jdk: 17
# We *are* the cyclic lib, don't try to load another one
cyclic_lib: -1

include_debug: true

packages:
- name: "cyclic_lib"
  version: "0.0.1"
  type: "jar"
  location: "../../libs/"